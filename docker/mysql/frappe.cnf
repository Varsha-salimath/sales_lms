# Force utf8mb4 for Frappe / LMS (required for emoji and full Unicode).
[mysqld]
character-set-client-handshake = FALSE
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

[mysql]
default-character-set = utf8mb4
